import 'package:flutter/material.dart';

class upcominmatches extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple[300],
      child: Center(
        child: Text('tab'),
      ),
    );
  }
}
